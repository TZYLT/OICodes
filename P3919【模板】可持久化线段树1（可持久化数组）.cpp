#include<bits/stdc++.h>
using namespace std;
#define M 1001000
vector<int> data[M],id[M];
int n,m;
int main(){
    scanf("%d%d",&n,&m);
    for(int i=1,x;i<=n;i++){
        scanf("%d",&x);
        id[i].push_back(0);
        data[i].push_back(x);
    }
    while(m--){
        int v,op,;
        scanf("%d",&op);
        if(op==1)
    }    
}